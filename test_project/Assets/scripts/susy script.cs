using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class susyscript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        bool x = true;
    }

    // Update is called once per frame
    void Update()
    {
        bool x = false;
    }
}
